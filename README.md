# Package Creation Tutorial

This project is a simple Python package built with Poetry.

## Features
- Reverse a string
- Count vowels in a string
- Capitalize words in a string

## Project structure
- `src/package_creation_tutorial`: package source code
- `tests`: unit tests with pytest

## Installation
```bash
python -m poetry install
